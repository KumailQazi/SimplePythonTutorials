# Integer Square Root
# This is the new function added to math library in Python.
#
# Return Value:
# Integer square root of the given nonnegative integer.
#
# Method:
# The isqrt() function internally uses the Newton’s iteration method.
#
import math

print(math.sqrt(5))
print(math.floor(math.sqrt(5)))
print(math.isqrt(5))
